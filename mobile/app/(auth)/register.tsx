import { ScreenPlaceholder } from "@/shared/components/layout/ScreenPlaceholder";

export default function RegisterScreen() {
  return <ScreenPlaceholder title="Create account" subtitle="Auth shell — implementation pending" />;
}
