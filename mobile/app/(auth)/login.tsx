import { ScreenPlaceholder } from "@/shared/components/layout/ScreenPlaceholder";

export default function LoginScreen() {
  return <ScreenPlaceholder title="Log in" subtitle="Auth shell — implementation pending" />;
}
