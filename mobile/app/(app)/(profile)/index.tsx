import { ScreenPlaceholder } from "@/shared/components/layout/ScreenPlaceholder";

export default function ProfileScreen() {
  return <ScreenPlaceholder title="Profile" subtitle="Settings shell — implementation pending" />;
}
